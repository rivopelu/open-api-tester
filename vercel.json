{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @modern-api-studio/client build",
  "outputDirectory": "apps/client/dist",
  "functions": {
    "api/[...route].ts": { "maxDuration": 30 }
  },
  "rewrites": [
    { "source": "/((?!api(?:/|$)).*)", "destination": "/index.html" }
  ]
}
